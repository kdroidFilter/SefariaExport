name: Python 3.9 + MongoDB + Sefaria Export (Self-hosted, all_merged → branch root)

on:
  workflow_dispatch:
    inputs:
      timezone:
        description: "Timezone for timestamp/tag (IANA name)"
        required: false
        default: "Asia/Jerusalem"

permissions:
  contents: write

concurrency:
  group: sefaria-export
  cancel-in-progress: false

env:
  TZ_NAME: ${{ inputs.timezone || 'Asia/Jerusalem' }}
  DJANGO_SETTINGS_MODULE: sefaria.settings
  MONGO_HOST: 127.0.0.1
  MONGO_PORT: 27017
  MONGO_DB_NAME: sefaria
  PYTHONDONTWRITEBYTECODE: 1

jobs:
  prep:
    name: Compute timestamp
    runs-on: [self-hosted, Linux, X64]
    outputs:
      stamp: ${{ steps.ts.outputs.stamp }}
    steps:
      - name: Compute release timestamp
        id: ts
        run: |
          TZ="${TZ_NAME}" date '+%Y-%m-%d_%H-%M' > ts.txt
          echo "stamp=$(cat ts.txt)" >> $GITHUB_OUTPUT
          echo "⏱  Using TZ=${TZ_NAME} -> $(cat ts.txt)"

  export_and_push:
    name: Export all_merged and push to date branch
    runs-on: [self-hosted, Linux, X64]
    needs: prep
    env:
      TS_STAMP: ${{ needs.prep.outputs.stamp }}
      SEFARIA_EXPORT_BASE: ${{ github.workspace }}/staging
      SEFARIA_EXPORT_PATH: ${{ github.workspace }}/staging

    steps:
      - name: Checkout .github (sparse only)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          ref: main
          sparse-checkout: |
            .github
          sparse-checkout-cone-mode: false

      # ───────── System prep ─────────
      - name: Ensure base tools & Docker (apt/dnf/yum/apk auto-detect)
        shell: bash
        run: |
          set -e
          if command -v apt-get >/dev/null 2>&1; then
            sudo apt-get update -y
            sudo apt-get install -y ca-certificates curl gnupg lsb-release \
              tar wget rsync coreutils netcat-openbsd git
            sudo apt-get install -y aria2 || true
            sudo apt-get install -y libre2-dev pybind11-dev build-essential cmake ninja-build || true
            # Install Docker if missing
            if ! command -v docker >/dev/null 2>&1; then
              echo "Installing Docker (apt)..."
              curl -fsSL https://get.docker.com | sudo sh
            fi
            sudo systemctl enable docker || true
            sudo systemctl start docker || true
          elif command -v dnf >/dev/null 2>&1; then
            sudo dnf install -y ca-certificates curl tar wget rsync coreutils nmap-ncat git
            sudo dnf install -y aria2 || true
            sudo dnf install -y re2-devel pybind11-devel gcc-c++ cmake ninja-build || true
            if ! command -v docker >/dev/null 2>&1; then
              curl -fsSL https://get.docker.com | sudo sh
            fi
            sudo systemctl enable docker || true
            sudo systemctl start docker || true
          elif command -v yum >/dev/null 2>&1; then
            sudo yum install -y ca-certificates curl tar wget rsync coreutils nmap-ncat git
            sudo yum install -y aria2 || true
            sudo yum install -y re2-devel pybind11-devel gcc-c++ cmake ninja-build || true
            if ! command -v docker >/dev/null 2>&1; then
              curl -fsSL https://get.docker.com | sudo sh
            fi
            sudo systemctl enable docker || true
            sudo systemctl start docker || true
          elif command -v apk >/dev/null 2>&1; then
            sudo apk add --no-cache ca-certificates curl tar wget rsync coreutils netcat-openbsd git
            sudo apk add --no-cache aria2 || true
            sudo apk add --no-cache re2-dev py3-pybind11 build-base cmake ninja || true
            if ! command -v docker >/dev/null 2>&1; then
              curl -fsSL https://get.docker.com | sudo sh
            fi
            sudo rc-update add docker default || true
            sudo service docker start || true
          fi
          echo "✅ System tools ensured; Docker version:"
          sudo docker version || true

      - name: Start MongoDB 6.0 in Docker (rootless not required)
        shell: bash
        run: |
          set -e
          # Always use sudo docker to bypass socket perms
          sudo docker rm -f gha-mongo >/dev/null 2>&1 || true
          sudo docker run -d --name gha-mongo -p 27017:27017 mongo:6.0
          # Wait for TCP
          for i in {1..120}; do
            if (echo > /dev/tcp/127.0.0.1/27017) >/dev/null 2>&1; then
              echo "✅ MongoDB TCP port is reachable"; exit 0
            fi
            echo "⏳ Waiting for MongoDB TCP..."; sleep 1
          done
          echo "❌ MongoDB did not become reachable"; exit 1

      - name: Ensure mongorestore (MongoDB Database Tools)
        shell: bash
        run: |
          if ! command -v mongorestore >/dev/null 2>&1; then
            echo "Installing MongoDB Database Tools..."
            TOOLS_VER="100.9.4"
            # Try Ubuntu build first
            URL="https://fastdl.mongodb.org/tools/db/mongodb-database-tools-ubuntu2204-x86_64-${TOOLS_VER}.tgz"
            wget -q "$URL" -O tools.tgz || true
            if [ -s tools.tgz ]; then
              tar -xzf tools.tgz
              sudo mv mongodb-database-tools-*/bin/* /usr/local/bin/
              rm -rf mongodb-database-tools-* tools.tgz
            fi
          fi
          mongorestore --version

      # ───────── Python 3.9 isolated venv ─────────
      - name: Install/Setup Python 3.9 (actions/setup-python)
        id: py
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          check-latest: true

      - name: Create & use venv
        shell: bash
        run: |
          set -e
          PYTHON="python"
          if ! command -v python >/dev/null 2>&1; then PYTHON="python3"; fi
          $PYTHON -V
          $PYTHON -m venv .venv
          . .venv/bin/activate
          python -m pip install --upgrade pip wheel setuptools
          echo "VIRTUAL_ENV=$PWD/.venv" >> $GITHUB_ENV
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      # ───────── Repo + deps Sefaria (no-bazel/no-pg_config) ─────────
      - name: Clone Sefaria-Project
        run: |
          git clone --depth 1 https://github.com/Sefaria/Sefaria-Project.git
          ls -la Sefaria-Project

      - name: Prepare constraints (force built-google-re2 & psycopg2-binary)
        working-directory: Sefaria-Project
        run: |
          # Replace google-re2 by prebuilt wheel; replace psycopg2 by psycopg2-binary
          awk '!/^\s*google-re2/ && !/^\s*psycopg2(\[\S+\])?(\s|==|$)/' requirements.txt > req-no-re2-no-psycopg2.txt
          echo "built-google-re2" >> req-no-re2-no-psycopg2.txt
          echo "psycopg2-binary" >> req-no-re2-no-psycopg2.txt
          mv req-no-re2-no-psycopg2.txt requirements.patched.txt
          echo "✅ Patched requirements ready"

      - name: Install Python deps in venv (no system packages)
        working-directory: Sefaria-Project
        env:
          PIP_NO_BUILD_ISOLATION: "0"
        run: |
          . "$GITHUB_WORKSPACE/.venv/bin/activate"
          python -V
          pip -V
          pip install --no-cache-dir -r requirements.patched.txt

      - name: Create minimal local_settings.py (sets SEFARIA_DB!)
        working-directory: Sefaria-Project/sefaria
        run: |
          . "$GITHUB_WORKSPACE/.venv/bin/activate"
          mkdir -p "$SEFARIA_EXPORT_PATH"
          cp local_settings_example.py local_settings.py
          python - <<'PY'
          import os, re
          p = "local_settings.py"
          with open(p, "r", encoding="utf-8") as f:
              s = f.read()
          s = re.sub(r"SEFARIA_EXPORT_PATH\s*=.*", f'SEFARIA_EXPORT_PATH = r"{os.environ["SEFARIA_EXPORT_PATH"]}"', s)
          s = re.sub(r"MONGO_HOST\s*=.*", f'MONGO_HOST = "{os.environ["MONGO_HOST"]}"', s)
          s = re.sub(r"MONGO_PORT\s*=.*", f'MONGO_PORT = {int(os.environ["MONGO_PORT"])}', s)
          s = re.sub(r"MONGO_DB_NAME\s*=.*", f'MONGO_DB_NAME = "{os.environ["MONGO_DB_NAME"]}"', s)
          if re.search(r"SEFARIA_DB\s*=", s):
              s = re.sub(r"SEFARIA_DB\s*=.*", 'SEFARIA_DB = "sefaria"', s)
          else:
              s += '\nSEFARIA_DB = "sefaria"\n'
          with open(p, "w", encoding="utf-8") as f:
              f.write(s)
          print("✅ local_settings.py configured (SEFARIA_DB=sefaria)")
          PY

      - name: Download small Mongo dump
        run: |
          mkdir -p mongo_dump
          if command -v aria2c >/dev/null 2>&1; then
            aria2c -x 16 -s 16 -k 1M -o dump_small.tar.gz "https://storage.googleapis.com/sefaria-mongo-backup/dump_small.tar.gz"
          else
            wget -O dump_small.tar.gz "https://storage.googleapis.com/sefaria-mongo-backup/dump_small.tar.gz"
          fi
          tar -xzf dump_small.tar.gz -C mongo_dump
          rm -f dump_small.tar.gz || true
          echo "Dump tree:"; find mongo_dump -maxdepth 3 | sed -e 's/^/  /'

      - name: Normalize layout to mongo_dump_pkg/sefaria
        run: |
          mkdir -p mongo_dump_pkg
          if [ -d mongo_dump/dump/sefaria ]; then
            cp -a mongo_dump/dump/sefaria mongo_dump_pkg/
          elif [ -d mongo_dump/sefaria ]; then
            cp -a mongo_dump/sefaria mongo_dump_pkg/
          else
            echo "❌ Could not find 'sefaria' folder in dump"; exit 1
          fi
          echo "Packaged tree:"; find mongo_dump_pkg -maxdepth 2 | sed -e 's/^/  /'

      - name: Restore DB and run export_all_merged
        env:
          PYTHONPATH: ${{ github.workspace }}/Sefaria-Project
        run: |
          set -euo pipefail
          . "$GITHUB_WORKSPACE/.venv/bin/activate"
          BASE="$SEFARIA_EXPORT_BASE"
          mkdir -p "$BASE"
          FINAL_DIR="$RUNNER_TEMP/final_root"
          mkdir -p "$FINAL_DIR"

          # Restore DB once from dump
          mongorestore --host 127.0.0.1 --port 27017 --drop --db sefaria "mongo_dump_pkg/sefaria" || { echo "❌ mongorestore failed"; exit 1; }

          python - <<'PY'
          from pymongo import MongoClient, errors
          client = MongoClient("mongodb://127.0.0.1:27017")
          db = client["sefaria"]
          try:
              db.create_collection("history")
              print("Created empty 'history' collection.")
          except errors.CollectionInvalid:
              print("'history' collection already exists.")
          PY

          # Point SEFARIA_EXPORT_PATH to staging/export_all_merged
          export CUR_EXPORT="export_all_merged"
          DEST_DIR="$BASE/$CUR_EXPORT"
          mkdir -p "$DEST_DIR"
          python - <<'PY'
          import os, re
          p = os.path.join("Sefaria-Project","sefaria","local_settings.py")
          with open(p, "r", encoding="utf-8") as f:
              s = f.read()
          dest = os.path.join(os.environ["GITHUB_WORKSPACE"], "staging", "export_all_merged")
          s = re.sub(r"SEFARIA_EXPORT_PATH\s*=.*", f'SEFARIA_EXPORT_PATH = r"{dest}"', s)
          with open(p, "w", encoding="utf-8") as f:
              f.write(s)
          print("➡️  SEFARIA_EXPORT_PATH set to:", dest)
          PY

          # Run export_all_merged
          python - <<'PY'
          import os, sys, django
          sys.path.insert(0, os.path.abspath("Sefaria-Project"))
          os.environ.setdefault("DJANGO_SETTINGS_MODULE", "sefaria.settings")
          django.setup()
          from sefaria import export as ex
          ex.export_all_merged()
          print("✅ export_all_merged completed successfully.")
          PY

          # Move export into FINAL_DIR
          rsync -a "$DEST_DIR/" "$FINAL_DIR/" || cp -a "$DEST_DIR/." "$FINAL_DIR/"

      - name: Drop DB and cleanup
        if: always()
        run: |
          set +e
          echo "🗑️ Dropping MongoDB database 'sefaria'..."
          sudo docker exec gha-mongo mongosh --quiet --eval 'db.getSiblingDB("sefaria").dropDatabase(); print("✅ Database dropped.")' 2>/dev/null || true
          sudo docker rm -f gha-mongo >/dev/null 2>&1 || true
          rm -rf mongo_dump mongo_dump_pkg Sefaria-Project || true
          rm -rf ~/.cache/pip || true
          echo "Disk usage after cleanup:"; df -h || true

      - name: Push exported content to date-named branch (root + keep .github)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          REPO_DIR="$RUNNER_TEMP/pushrepo"
          rm -rf "$REPO_DIR" && mkdir -p "$REPO_DIR"
          git -C "$REPO_DIR" init
          git -C "$REPO_DIR" config user.name "github-actions[bot]"
          git -C "$REPO_DIR" config user.email "github-actions[bot]@users.noreply.github.com"
          git -C "$REPO_DIR" remote add origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          TS_BRANCH="${TS_STAMP}"
          git -C "$REPO_DIR" checkout --orphan "$TS_BRANCH"
          # Preserve only the CI workflows
          mkdir -p "$REPO_DIR/.github"
          cp -a "$GITHUB_WORKSPACE/.github/." "$REPO_DIR/.github/"
          # Copy exported content directly into repo root
          FINAL_DIR="$RUNNER_TEMP/final_root"
          cp -a "$FINAL_DIR/." "$REPO_DIR/"
          cd "$REPO_DIR"
          git add -A
          if git diff --cached --quiet; then
            echo "No changes to push; skipping commit."
          else
            git commit -m "chore(export): export_all_merged at ${TS_STAMP} [skip ci]"
            git push --force origin HEAD:"$TS_BRANCH"
            echo "✅ Pushed to branch ${TS_BRANCH}"
          fi
